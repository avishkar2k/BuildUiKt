<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/fl"
    android:layout_width="match_parent"
    android:layout_height="@dimen/_height_fl"
    android:background="@android:color/background_dark">

    <TextView
        android:id="@+id/tv_team_name"
        style="@style/appTextStyleTitle"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_margin="@dimen/box_inset_layout_padding"
        android:drawablePadding="@dimen/box_inset_layout_padding"
        android:text="@string/rcb"
        android:textColor="@android:color/holo_green_light"
        app:drawableEndCompat="@drawable/arrow_collapsed" />

</FrameLayout>